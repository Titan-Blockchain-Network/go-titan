.DS_Store
.idea/
local-scripts/
plans/
audits/

# Titan blockchain sensitive / runtime data
titan-staking/
titan-keys/
*-staking/
data/
db/
logs/
*.key
staker.crt
staker.key
signer.key

# Common node runtime artifacts
tmp/
tmp-*/
build/
*.log
*.pid
db-*
logs-*

# Network genesis (generated per deployment — never commit)
titan-network/origin.json

# Local Go build cache (smoke-test / install-dev)
.gopath/
.gomodcache/
avalanchego/test-results/

# Node modules (legacy)
node_modules/
.env.local